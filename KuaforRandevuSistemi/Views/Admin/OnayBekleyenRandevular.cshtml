@model IEnumerable<Randevu>

<div class="container">
    <h2>Onay Bekleyen Randevular</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Tarih</th>
                <th>Müşteri</th>
                <th>Çalışan</th>
                <th>İşlem</th>
                <th>Onay Durumu</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var randevu in Model)
            {
                <tr>
                    <td>@randevu.Tarih</td>
                    <td>@randevu.Musteri.Ad @randevu.Musteri.Soyad</td>
                    <td>@randevu.Calisan.Ad @randevu.Calisan.Soyad</td>
                    <td>@randevu.Islem.Ad</td>
                    <td>@(randevu.Onaylandi ? "Onaylandı" : "Beklemede")</td>
                    <td>
                        @if (!randevu.Onaylandi)
                        {
                            <form asp-action="Onayla" method="post">
                                <input type="hidden" name="id" value="@randevu.Id" />
                                <button type="submit" class="btn btn-success">Onayla</button>
                            </form>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
